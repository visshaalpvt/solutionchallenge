rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read/write for the demo.
    // For production, this should be restricted to authenticated users.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
